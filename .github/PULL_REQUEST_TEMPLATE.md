<!-- Thanks for contributing. Keep PRs focused and small where you can. -->

## What does this change?

<!-- A short description, and the issue it closes (e.g. "Closes #12"). -->

## Checklist

- [ ] `ruff check src/ tests/` passes
- [ ] `pytest` passes (`PYTHONPATH=src pytest tests/`)
- [ ] I did not commit secrets, model weights, or generated images
- [ ] Docs updated if behaviour changed
